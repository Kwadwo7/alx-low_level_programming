#include "main.h"

/**
 * get_endianness - checks the littleness or bigness of a machine
 * endian
 * Return: 0 for big and 1 for little
 */

int get_endianness(void)
{
	unsigned int k;
	char *b = (char *) &k;

	k = 1;
	return (*c);
}
