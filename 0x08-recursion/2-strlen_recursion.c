#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - entry function
 * @s: string input
 * Return: 0 for success
 */

int _strlen_recursion(char *s)
{
	int len = strlen(s);
	
	return len;
}
