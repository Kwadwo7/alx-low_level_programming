#include "main.h"
#include <math.h>

/**
 * _pow_recursion - entry of function
 * @x: input value
 * @y: input value
 * Return: 0 for success
 */

int _pow_recursion(int x, int y)
{
	int power = pow(x, y);

	return (power);
}
